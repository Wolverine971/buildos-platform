<!-- tasker/41-open-brief-cohort-1.md -->

# 41 — Open-brief cohort 1: the number the orchestration bet has been waiting for

**Created 2026-07-27.** Owner: a fresh agent session.
**Type:** evaluation build + paid cohort. No durable infrastructure.
**Status:** IN PROGRESS (started 2026-07-29). The DJ veto packet is prepared and awaiting a
numbers-only pass. The $0 instrument, corpus-readiness gate, pre-registered 36-output run plan,
three-lane blind packet/readout, frozen fixtures, and inert production-control runner are built and
locally verified; workflow + single-agent lane runners and paid execution remain. Handoff remains:
`docs/architecture/agent-first-orchestration/OPEN_BRIEF_COHORT_HANDOFF_2026-07-27.md` —
**read it in full before doing anything; it contains the reading order, six work packages,
landmines, and the exit condition.**

## The one-sentence mission

Score the first three-lane open-brief cohort — production v2 chat (control) vs the Phase A
workflow lane vs a single strong agent — through L0–L2 machine checks plus the new feasibility
self-assessment check, and put a counterbalanced blind packet in front of DJ.

## Why this exists

Phase A closed instrument-limited (`PHASE_A_RESULTS.md`) with the quality half never scored.
The open-brief instrument inherited the question. DJ's acceptance bar is captured verbatim in
`corpus/open-brief-v1.json`; three small inputs remain his (3 dictated briefs, 5 label
sign-offs, the Tacemus call) and MUST be collected as a veto packet — DJ is in background mode,
agents prepare and he fires.

## Exit

`OPEN_BRIEF_RESULTS_COHORT1.md` with per-layer scores per lane + cost/latency ledger +
pre-registered decision rule readout; corpus `pending_from_dj` empty or explicitly deferred;
DJ holding the blind packet and a one-page decision brief. Cohort 1 establishes distributions —
no Go-class conclusion on its denominator.
