// apps/web/src/lib/server/agent-call/agent-call-project-activity.service.ts
// Shim: carved into @buildos/shared-agent-ops (Wave 7). Worker-safe; takes the
// admin client as a param. Kept here so existing $lib importers don't churn.
export * from '@buildos/shared-agent-ops/gateway/agent-call-project-activity.service';
