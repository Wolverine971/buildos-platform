// apps/web/src/lib/services/agentic-chat/tools/core/definitions/field-metadata.ts
/**
 * Entity Field Metadata
 *
 * The curated ENTITY_FIELD_INFO catalog moved to
 * @buildos/agentic-chat-runtime/tools (Phase 4 Slice 18 S3-T5) so the shared
 * get_field_info implementation can serve it on both web and the worker. This
 * shim preserves the legacy web import path.
 */

export { ENTITY_FIELD_INFO } from '@buildos/agentic-chat-runtime/tools';
