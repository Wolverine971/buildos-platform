// apps/web/src/lib/services/ontology/document-write.service.ts
// Re-export shim. The worker Agent Run runner and web routes must use the same
// guarded document head + version write behavior.
export * from '@buildos/shared-agent-ops/ontology/document-write.service';
