---
title: 'How to Slay with CSS'
video_id: XffXBuqvWYI
url: 'https://www.youtube.com/watch?v=XffXBuqvWYI'
channel: Hyperplexed
channel_url: 'https://www.youtube.com/@Hyperplexed'
upload_date: 2026-05-12
duration: '06:03'
views: 36900
description: |
    Get the best UI/UX inspiration with Mobbin: https://rebrand.ly/5215bf

    Demo: https://rebrand.ly/wxjpj1n
    Code: https://rebrand.ly/vxcafm3
transcribed_date: '2026-07-01'
path: apps/web/docs/technical/components/hyperplexed/transcripts/how-to-slay-with-css.md
---

# How to Slay with CSS

## Metadata

- **Channel**: [Hyperplexed](https://www.youtube.com/@Hyperplexed)
- **Video**: [Watch on YouTube](https://www.youtube.com/watch?v=XffXBuqvWYI)
- **Duration**: 06:03
- **Upload Date**: 2026-05-12
- **Views**: 36,900

## Transcript

The other day, I was playing one of my favorite video games when I had a sudden realization. Why would I continue playing this game when I could just recreate it with CSS? So, I got to work. The first thing I'll need is an arm, but rather than just rip theirs, I'm going to go to Canva and type arm pointing. Hmm, not exactly what I was looking for. I suppose I could try hand pointing. Okay, I could work with this aesthetic, but now I'll need to find the arm piece. Excuse me a moment while I work in a real time here to attach it, crop it down, and export. [music] By the way, there will be no sponsored segment today thanks to Mabin. They are awesome and you should check them out in the description below. Now that I've built my own custom arm, I can begin coding my card selection interface. I know I'm going to need a container and I'm going to put eight cards inside. I'll use display grid to orient the cards into two rows of four, and rather than letting the cards define their own size, I'm just going to enforce the standard aspect ratio of a playing card, which is 5 by 7, and leave the width up to the container. I could use a fixed unit like pixels or rem here, but since video games can typically be played on many different screen sizes, I want it to be a little more responsive. There are a few different ways we could do that, but I'm going to keep it simple for the sake of this video and just use the viewport width unit as a quick way to get everything to scale proportionally with the width of the window. Moving on to the arm, let's pop it down directly alongside the cards, give it a position value of absolute, and put relative on the container because I said so. Now, in the original, the arm starts out of view, and when an item is hovered, it transitions to that item's respective location. We could write a fancy pants script to dynamically calculate position values, but since there are a fixed number of item slots that don't ever change, I'm literally just going to hardcode some values in my stylesheet. For the starting position, we'll go 50% of the arm and 50% of the frame, and 100% reason to remember the name. Then, for each of the card slots, we'll say, if the container has that specific card number currently being hovered, then move the arm to its corresponding position. Once we have the positioning locked down, we can add a transition duration for the left and top properties. Ah, it looks like our arm is a little quick to jump back to the default position. So, let's add a short transition delay. Though, that makes the hover state feel a little laggy. We really only need the delay on dehover, not on rehover. So, let's say if the container currently has a card that is actively being hovered, set the delay for the arm transition to zero. And if that condition is ever false, like when your mouse is in between cards or you move it away completely, then the longer delay value has us covered. And finally, we need to fix the whole floating arm thing by setting overflow to hidden on the container. Now, if this were a real game with a need for tracking real game state, like what items are available to the player and what cards they've currently selected, I would be inclined to set up a back end to manage that data server-side and to use my favorite framework to manage interactivity on the client. But, since this is just a YouTube video, I get the privilege of not having to worry about any of that at all. So, here's what I'm going to do. Let's convert the cards from divs to buttons and create a loop to iterate over them and add an on-click handler to each one. When a card is clicked, I'll set a selected attribute on its data set to true. I can then use that attribute in my style sheet to select the cards currently in that state and apply a translation to shift them out of view. Now, when I'm designing an interface like this one, I like to add a lot of micro-interactions that barely make a difference functionally, but that I personally find makes the overall experience more enjoyable. We can start with the easy ones, like scaling up on hover state and down on active state. And then there's the more subtle ones, like the timing function used for the exit transition. The default value of ease is typically fine for basic interactions, but for a fancy card selector like ours here, it sort of feels unnatural to the eye. However, if we drag the timing curve over this way, actually dipping a bit into the negative progression space at the start, what the brain perceives as anticipation before the takeoff, well, me monkey brain likey. And finally, even though the selected cards are out of sight, they're technically still interactable via tapping, so we can fix that issue in the on click handler by setting the disabled property to true. Next up on the list of to-do's is what happens to the cards after they get selected. In the original, they animate out of view and increment a counter in the toolbar up top, but in order to keep this demo fairly contained for hours, I want to create a card deck [music] that fills up as the user makes selections, and upon clicking a card in this deck, it should return back to its original position. So, we'll need another container in the shape of a card that is absolutely positioned in the bottom right corner of the screen. When a card gets clicked, we'll append a button element to this container. The first problem we're going to run into is that the cards all appear instantaneously rather than animating into view. To fix this, upon creation of each button, we can apply a hidden data attribute that, when set to true, translates the button out of view after a short delay, leaving enough time for the card to make its exit, we can flip this value to false, thereby triggering its transition into view. The next issue is that there's no way to tell that there are multiple cards currently selected. So, upon creation, let's assign each card an index that we can then use to translate each card slightly to the left of the previous one. With that out of the way, we've successfully completed the transition from container to deck, but we still need a way to get the cards back to their original positions. Fortunately, since the cards never actually left the container, we can simply do the exact opposite of what we did before, and upon clicking a card in the deck, we can select the corresponding card from the container and flip the two properties we changed back to their default values. At the same time, we can reapply the hidden property to the deck card, and after we've given [music] it enough time to complete its transition, we can remove it from the DOM. Now, if this were a real game, I'd probably commission someone to make actual game art, but for now, I'll have to settle for some icons and extra stylings via CSS. I'll leave a link to the code for this project in the description. Thanks for watching, and I'll see you in the next one.
