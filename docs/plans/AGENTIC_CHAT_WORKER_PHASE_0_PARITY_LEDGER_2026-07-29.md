<!-- docs/plans/AGENTIC_CHAT_WORKER_PHASE_0_PARITY_LEDGER_2026-07-29.md -->

# Agentic Chat Worker Migration: Phase 0 Parity Ledger

**Date:** 2026-07-29

**Status:** Phase 0 operator evidence is complete and independent re-acceptance remains pending. Phase 1 implementation and hosted schema deployment are complete under the explicit waiver recorded in the closure checklist. The Phase 1 hosted quality rerun failed 3 of 24 scenario executions, so Phase 1 is not exited and Phase 2 remains blocked.

**Parent plan:** `docs/plans/AGENTIC_CHAT_WORKER_REALTIME_MIGRATION_PLAN_2026-07-29.md`

## Purpose

This ledger prevents the worker migration from treating the current model loop as the whole product. The live route owns authentication, session and context resolution, prepared inputs, history, attachments, tools, quality safeguards, persistence, cancellation, telemetry, and finalization in addition to calling `streamFastChat`.

Each row names the current owner, the intended target boundary, and the test that must prove parity. `Covered` means a current local regression test exists; it does not mean the worker adapter exists. `Gap` means the current behavior or test is insufficient for the worker contract.

## Phase 1 implementation delta — 2026-07-31

The row-by-row ledger below preserves the Phase 0 baseline. These local Phase 1 changes now supersede the corresponding legacy-owner descriptions without changing the worker target:

- **P06–P09/P12/P13:** `081d43641` replaced split legacy admission with the service-only `admit_legacy_agentic_chat_turn(...)` transaction. It resolves duplicates before active-turn checks, snapshots fallback history before the current message, and commits one running turn plus one linked user message. The detached admitted-user-message insert is gone. Prepared content is consumed only for `newly_admitted`, and prepared history still wins only under the existing validity/divergence rules.
- **P21/P22:** `38ad9fd9d` routes legacy public events through the transport-neutral event-sink port. `9047992bc` derives the runtime event payload from the complete `AgentSSEMessage` union and proves every current variant is representable. Event identity remains legacy stream-scoped; generation-aware durable ordering is still Phase 2 work.
- **Browser lifecycle/cancellation:** `947d2a726` stores one discriminated `TurnHandleV1` and uses it for cancel, detach, and reconcile. Legacy acknowledgements hydrate session/turn IDs; worker handles fail closed because worker transport is not enabled.
- **P30 writer boundary:** `38086f010` makes the observability writer server-only and moves `chat_prompt_snapshots` inserts onto the service-role client with active user/session/turn scope checks. `7c0e1eb87` removes the obsolete direct legacy admission DML bypass.
- **P10/P26/P30/P37 server boundaries:** `94bb04e62` enforces `.server.ts` ownership for prepared-prompt consumption, checkpoints, and admin replay; adds a service-role prepared-content store; routes replay-source mutation through the observability writer; and proves authenticated clients cannot access prepared content. It also adds a normalized cold/prepared SSE-persistence differential and a fresh two-connection PostgreSQL admission runner.
- **Hosted schema/types:** migration `20260731150000` is applied and recorded on the configured hosted project; RPC drift is green at 195 functions. `c5472a276` regenerates the hosted database types, with only the live admission RPC's function ordering changing.
- **Hosted adapter regression:** the first pre-model hosted preflight exposed a detached `supabase.rpc` receiver. `9521d814f` binds the RPC method to its Supabase client and adds a receiver-dependent regression test; the corrected clean tree admitted and completed all 30 hosted turns.
- **Shared extraction foundation:** `0801da418` adds the portable `@buildos/agentic-chat-runtime` contracts/ports package; it contains no worker processor or deployment binding.

Current local evidence is 134 Phase 1 web tests across 17 files before the hosted adapter fix, a post-fix 45/45 focused admission/route run, 4 runtime-package tests, 13 shared-types tests, clean runtime/shared/web typechecks, a passing fresh disposable PostgreSQL integration, and a passing normalized legacy differential. Hosted migration, generated types, and RPC drift are aligned.

All planned Phase 1 code boundaries, local gates, and hosted schema gates are complete. The remaining exit evidence is the hosted quality bar: the first clean retry-free attempt passed 21/24 scenarios and 27/30 turn assertions, below the retained Phase 0 baseline's 24/24 and 30/30. Phase 2 remains blocked until a subsequent complete clean cohort passes.

## Current control points

| Concern                        | Current source of truth                                                                       | Important current behavior                                                                                                                                                                  |
| ------------------------------ | --------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| HTTP lifecycle                 | `apps/web/src/routes/api/agent/v2/stream/+server.ts`                                          | Authenticates, validates, resolves context, starts a detached async turn, returns SSE.                                                                                                      |
| Model/tool loop                | `apps/web/src/lib/services/agentic-chat-v2/stream-orchestrator/`                              | Streams models, selects/executes tools, repairs invalid work, supervises long turns, synthesizes final output.                                                                              |
| Session/history/message writes | `session-service.ts`, legacy admission projector/RPC                                          | Resolves sessions; atomic admission owns the current user message and pre-message fallback snapshot.                                                                                        |
| Admission                      | `turn-admission.ts`, `admit_legacy_agentic_chat_turn(...)`                                    | Service-only duplicate-first transaction owns stale reclaim, turn insert, user-message insert, and linkage.                                                                                 |
| Prepared prompt                | `prepared-prompt-consumer.server.ts`, `prepared-prompt-store.server.ts`, `prewarm/+server.ts` | Authenticated product/history reads remain scoped; prepared content is created, read, claimed, and consumed only through the service-role store, with bounded metadata returned to clients. |
| Live event envelope            | `stream-events.ts`, `packages/shared-types/src/agent.types.ts`                                | Adds stream-scoped identity and sequence metadata to SSE events. No execution generation exists.                                                                                            |
| Durable events/status          | `turn-observability-writer.server.ts`                                                         | Service-role writer buffers semantic events, patches the turn row, owns scoped prompt-snapshot inserts, and owns the admin replay-source patch.                                             |
| Cancellation                   | `stream/cancel/+server.ts`, `cancel-reason-channel.ts`                                        | Writes a best-effort session metadata hint and uses an in-process hint channel.                                                                                                             |
| Browser lifecycle              | `agent-chat-stream-controller.svelte.ts`                                                      | Owns optimistic send and legacy SSE while retaining one `TurnHandleV1` for cancel/detach/reconcile.                                                                                         |
| UI event application           | `agent-chat-sse-handler.ts`                                                                   | Applies text, tools, phases, context shifts, timing, errors, and terminal events.                                                                                                           |
| General worker queue           | `apps/worker/src/lib/supabaseQueue.ts`                                                        | Processing-token claim, per-slot refill, heartbeat, timeout abort, generic retry, and generic stalled reset.                                                                                |

## Behavioral parity ledger

| ID  | Product behavior that must not regress                                                                                              | Current owner/evidence                                                                                                                                                                                                                        | Target owner                                                                                                                                                                                                             | Proving test and phase                                                                                              | State                                                                                   |
| --- | ----------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------- |
| P01 | Authenticate the caller before any durable turn work.                                                                               | Stream route `safeGetSession`; route tests cover unauthorized GET/POST behavior.                                                                                                                                                              | Web gateway only. Worker revalidates stored ownership before execution.                                                                                                                                                  | Route auth tests in Phase 1; forged-command integration test in Phase 2.                                            | Covered                                                                                 |
| P02 | Normalize one canonical request, including legacy aliases, context target, message, and IDs.                                        | `stream-request.ts`, `types.ts`, route parsing.                                                                                                                                                                                               | Web gateway request adapter plus versioned command builder.                                                                                                                                                              | Existing stream-request tests; canonical hash fixtures in Phase 0/2.                                                | Phase 0 hash fixture complete; adapter gap                                              |
| P03 | Validate attachment count, ownership, storage references, OCR metadata, and model-facing text.                                      | Stream route, `stream-attachments.ts`, `attachments.ts`.                                                                                                                                                                                      | Web gateway validates and freezes normalized refs; runtime attachment port consumes the artifact.                                                                                                                        | Existing attachment tests; immutable-artifact attachment test in Phase 2; live-vision parity in Phase 4.            | Covered + worker gap                                                                    |
| P04 | Preserve attachment-only display text and current-message model content.                                                            | Stream route builds `storedUserMessageContent` and `messageForModel`.                                                                                                                                                                         | Shared command normalization and prompt-input port.                                                                                                                                                                      | Differential prompt fixture in Phase 1/4.                                                                           | Legacy fixture complete; runtime differential pending                                   |
| P05 | Authorize project and daily-brief scope before session/admission.                                                                   | `access-checks.ts` plus route guards.                                                                                                                                                                                                         | Web gateway, with worker relationship revalidation from stored IDs.                                                                                                                                                      | Existing access tests; cross-tenant RPC tests in Phase 2.                                                           | Covered + worker gap                                                                    |
| P06 | Resolve an existing session or create the correct first-turn session.                                                               | `session-service.resolveSession`; client may currently allow a prepared prompt to defer creation to the stream.                                                                                                                               | Legacy admission RPC; worker session-inline admission (resolve-or-create inside `create_agentic_chat_turn_with_job`, keyed `(user_id, client_turn_id)`; contract rev .3).                                                | Existing controller/session tests; concurrent inline-creation race and rollback-leaves-no-session tests in Phase 2. | Covered + atomicity gap                                                                 |
| P07 | Enforce one active turn per session and reclaim genuinely stale legacy turns.                                                       | `turn-admission.ts`; partial unique index covers only `running`.                                                                                                                                                                              | Duplicate-first legacy/worker admission RPCs; queued and running index; chat-specific sweeper.                                                                                                                           | Existing admission tests; concurrent duplicate/active-turn DB tests in Phase 1/2.                                   | Covered + correctness gap                                                               |
| P08 | Idempotently bind a client turn to one turn and one user message.                                                                   | Turn insert and `persistMessage()` are separate; message key is `turn:<client_turn_id>:user`.                                                                                                                                                 | Atomic legacy admission and atomic worker admission; keys derive from durable `turn_run_id`.                                                                                                                             | Lost-response/concurrent duplicate DB tests in Phase 1/2.                                                           | Gap                                                                                     |
| P09 | Do not consume a prepared prompt when admission loses the active-turn race.                                                         | Route admits before `consumePreparedPrompt`; route test covers this ordering.                                                                                                                                                                 | Duplicate-first admission transaction claims/copies prepared input only for a genuinely new admitted turn.                                                                                                               | Existing route test; DB conflict/no-side-effect tests in Phase 2.                                                   | Covered + transaction gap                                                               |
| P10 | Verify prepared prompt ownership, nonce, expiry, session/scope, surface, and harness integrity.                                     | `prepared-prompt-consumer.server.ts` and `prepared-prompt-store.server.ts`; service-role content access with bounded client metadata.                                                                                                         | Server-only prepared-input service and admission RPC; browser receives metadata only.                                                                                                                                    | Existing prepared-prompt tests plus authenticated-client denial/tamper tests; artifact durability remains Phase 2.  | Covered for legacy + durable-artifact gap                                               |
| P11 | Preserve prepared context/history through queue delay and cleanup.                                                                  | Prepared row expires after a short TTL and is updated as consumed; no immutable turn artifact exists.                                                                                                                                         | `chat_turn_input_artifacts` with version/hash/retention and explicit stale-context policy.                                                                                                                               | TTL cleanup, hash mismatch, and delayed-claim tests in Phase 2.                                                     | Gap                                                                                     |
| P12 | Compose bounded history with compression, continuity hints, attachment context, interrupted-tool recovery, and loaded-skill ledger. | `session-service.loadRecentMessages`, `history-composer.ts`, `prepared-prompt-history.ts`.                                                                                                                                                    | Shared history port reading the immutable normalized snapshot.                                                                                                                                                           | Existing history/session tests; legacy differential and source-mutation tests in Phase 1/2.                         | Exact legacy projection + immutable fixture complete; worker gap                        |
| P13 | Include the current user input exactly once.                                                                                        | Route loads history before its detached user-message insert, then appends `messageForModel` for the current turn.                                                                                                                             | Admission freezes pre-message history and defensively excludes `user_message_id`; runtime adds current input once.                                                                                                       | Exact-once delayed/retry fixture in Phase 2 and differential suite in Phase 4.                                      | Route exact-once + artifact exclusion fixtures complete; DB retry gap                   |
| P14 | Resolve turn intent, pending intent, domain sensing, cache bypass, and tool surface.                                                | `turn-preparation.ts` plus route-owned overlays.                                                                                                                                                                                              | Shared preparation service with session/context ports.                                                                                                                                                                   | Existing turn-preparation/intent/tool-selector tests; normalized event/prompt snapshot comparison in Phase 1/4.     | Covered + extraction gap                                                                |
| P15 | Build the lite prompt, prepared-surface overlay, active situation rules, skill preload, and scaffold fingerprint.                   | Route plus `agentic-chat-lite/prompt`, domain and skill services.                                                                                                                                                                             | Shared prompt-construction service.                                                                                                                                                                                      | Existing prompt/cache/scaffold tests; prompt snapshot golden comparison in Phase 1/4.                               | Covered + extraction gap                                                                |
| P16 | Preserve context loading/cache semantics and entity-resolution hints.                                                               | `context-loader.ts`, `context-cache*`, `entity-resolution.ts`, route.                                                                                                                                                                         | Context port implemented by web/worker adapters; accepted prepared context copied to input artifact.                                                                                                                     | Existing context tests; prepared/cold-cache differential fixtures in Phase 1/4.                                     | Covered + extraction gap                                                                |
| P17 | Preserve live vision, signed image preparation, and bounded raw-media use.                                                          | Route and `stream-attachments.ts`.                                                                                                                                                                                                            | Gateway attachment preparation plus runtime multimodal input port.                                                                                                                                                       | Existing unit tests; isolated attachment/live-vision parity tests in Phase 4.                                       | Covered + worker gap                                                                    |
| P18 | Preserve model tiering, pinned eval models, forced synthesis routing, token limits, and provider metadata.                          | Route configuration plus orchestrator.                                                                                                                                                                                                        | Shared LLM port and runtime policy input.                                                                                                                                                                                | Existing model-tiering/orchestrator tests; deterministic adapter comparisons in Phase 4.                            | Covered + extraction gap                                                                |
| P19 | Preserve direct and batch tool execution, project-id injection, context scope, validation, and abort signal.                        | Route adapters, `ToolExecutionService`, `ChatToolExecutor`, orchestrator.                                                                                                                                                                     | Shared tool port with worker adapter and combined signal.                                                                                                                                                                | Existing tool/orchestrator tests; cancel-between-stages and context-scope parity tests in Phase 3/4.                | Covered + worker gap                                                                    |
| P20 | Persist mutating executions early enough for crash diagnosis.                                                                       | Route writes mutation rows incrementally, then bulk reconciles at terminal.                                                                                                                                                                   | Stable effect reservation/begin/receipt ledger plus fenced tool-execution writer.                                                                                                                                        | Existing persistence tests; effect lifecycle and crash matrix in Phase 2/5.                                         | Partial; safety gap                                                                     |
| P21 | Emit tool call/result, skill activity, context usage, phases, context shifts, last-turn context, timing, errors, and done.          | Route callbacks and `stream-events.ts`; handler applies the live union.                                                                                                                                                                       | Transport-neutral `AgentStreamEvent` through SSE or Realtime sinks.                                                                                                                                                      | Existing stream/handler tests; normalized legacy/worker event log in Phase 1/4.                                     | Covered + contract gap                                                                  |
| P22 | Order and deduplicate events.                                                                                                       | SSE uses `<stream_run_id>:<sequence>`; client guard is stream-scoped and lenient.                                                                                                                                                             | `<turn_run_id>:<generation>:<sequence>` with database-authoritative progression and generation reset.                                                                                                                    | Existing protocol tests; stale generation/gap/reconcile tests in Phase 2.                                           | Phase 0 identity fixture complete; DB gap                                               |
| P23 | Continue execution after browser/SSE detaches.                                                                                      | `sendTimedMessage` marks `streamDetached` and turn execution continues; controller reconciliation is session based.                                                                                                                           | Worker is independent of the browser; Realtime is acceleration only.                                                                                                                                                     | Existing controller detach tests; worker disconnect/reconnect test in Phase 3.                                      | Covered semantics + worker proof required                                               |
| P24 | Explicit Stop aborts model and tool I/O and persists a partial result when nonempty.                                                | Route combines timeout/cancel hint in one `AbortController`; cancellation persists a partial assistant message.                                                                                                                               | Durable signal, active controller registry, one batched observer, terminal CAS.                                                                                                                                          | Existing abort tests; queued/stream/tool-stage/cancel-race tests in Phase 2/3.                                      | Partial; durability gap                                                                 |
| P25 | Supersede does not overlap turns.                                                                                                   | Client waits at most 120 ms for a cancel-hint request, aborts locally, then starts the replacement. DB running index may reject the replacement.                                                                                              | Cancel old handle, wait for durable terminal evidence, then negotiate/admit replacement.                                                                                                                                 | Current controller test documents behavior; ignored-abort and terminal-delay tests in Phase 2/5.                    | Gap                                                                                     |
| P26 | Preserve supervisor decisions, checkpoint creation, resume, restoration, and completion.                                            | Orchestrator supervisor plus enforced `checkpoint-service.server.ts`; the stream passes the service-role client.                                                                                                                              | Shared supervisor/checkpoint port using fenced server-only RPCs.                                                                                                                                                         | Existing supervisor/checkpoint tests; stale-generation write tests in Phase 2; differential tests in Phase 4.       | Covered for legacy + worker-fence gap                                                   |
| P27 | Preserve deterministic research capture.                                                                                            | Route calls `buildResearchEntryFromCalls` and awaits `appendResearchEntry`.                                                                                                                                                                   | Shared deterministic post-processing port.                                                                                                                                                                               | Research-log E2E scenario and deterministic fixture in Phase 1/4.                                                   | Phase 0 fixture complete; finalization and readback passed the corrected hosted gate 3× |
| P28 | Preserve deterministic stated-future forward carry.                                                                                 | Route calls `createStatedFutureTask` from actual user text/tool executions.                                                                                                                                                                   | Shared deterministic post-processing port.                                                                                                                                                                               | Forward-carry E2E scenario and deterministic fixture in Phase 1/4.                                                  | Phase 0 fixture complete; forward carry passed the corrected hosted gate 3×             |
| P29 | Preserve outcome resolution, pending intent, domain session-state merge, skill gate telemetry, and finalization guard metadata.     | Route finalization block and orchestrator result.                                                                                                                                                                                             | Shared outcome/post-processing services plus telemetry port.                                                                                                                                                             | Existing outcome/orchestrator tests; persistence snapshot comparison in Phase 1/4.                                  | Covered + extraction gap                                                                |
| P30 | Persist prompt snapshots, semantic events, timing, cost attribution, and first-lane markers.                                        | Route plus `TurnObservabilityWriter`; several writes are detached/best effort.                                                                                                                                                                | Runtime telemetry port and fenced worker writers; terminal transaction owns terminal projection.                                                                                                                         | Existing observability tests; normalized persistence comparison in Phase 1/4.                                       | Covered + fence/atomicity gap                                                           |
| P31 | Persist exactly one final assistant message before successful completion is visible.                                                | Assistant message is awaited before `done`, but `chat_turn_runs` terminal update happens after `done` in a separate write.                                                                                                                    | One terminal CAS commits message, stream state/event, and status before terminal Broadcast.                                                                                                                              | Assistant-persist-failure route test; completion/cancel lock-order tests in Phase 2.                                | Truth-table fixture complete; DB linearization gap                                      |
| P32 | Reconcile agent state and session context after the turn.                                                                           | Route starts agent-state reconciliation asynchronously and updates session context.                                                                                                                                                           | Shared post-processing port with explicit bounded durability/telemetry policy.                                                                                                                                           | Existing sanitization/session tests; persistence parity in Phase 4.                                                 | Partial; detached-write gap                                                             |
| P33 | Preserve voice-note group and message-attachment linkage.                                                                           | Session service links these after user message persistence.                                                                                                                                                                                   | Admission/finalization attachment ports using admitted IDs and idempotent writers.                                                                                                                                       | Existing session/attachment tests; worker parity in Phase 4.                                                        | Partial                                                                                 |
| P34 | Roll back an optimistic bubble only when admission was rejected before persistence.                                                 | SSE `error.turn_rejected` plus controller evidence guard.                                                                                                                                                                                     | Typed transport admission result; same UI event application layer.                                                                                                                                                       | Existing controller rejection tests; worker capacity/idempotency tests in Phase 2/3.                                | Covered + adapter gap                                                                   |
| P35 | Keep legacy SSE as the rollback path for new turns without switching admitted turns.                                                | The controller stores a fixed legacy `TurnHandleV1`; worker handles fail closed and no transport lease exists yet.                                                                                                                            | Server-issued lease and immutable `TurnHandle`.                                                                                                                                                                          | Legacy handle/cancel/reconcile tests in Phase 1; lease/flag/lost-response tests in Phase 2.                         | Legacy handle covered; lease/worker gap                                                 |
| P36 | Enforce the consumption-billing/frozen-account guard for every chat turn's AI spend (audit F10).                                    | `consumption-billing.ts` classifies `/api/agent/` as `ai_compute`; `hooks.server.ts` re-evaluates the gate after the response — which in worker mode fires at the 202, before any model call.                                                 | Gateway pre-check at admission plus a worker-side gate re-evaluation RPC at terminal finalization (**decided 2026-07-30: option A**; credit pre-reservation/settlement deferred until paying-user overage justifies it). | Frozen-account admission rejection and finalization gate re-evaluation tests in Phase 4.                            | Gap (uncovered before audit)                                                            |
| P37 | Keep the admin prompt replay/eval runner working as a programmatic `/stream` caller (audit F22).                                    | Enforced `prompt-replay-runner.server.ts` posts the existing replay IDs and routes the `chat_turn_runs.source` patch through the service-role observability writer while retaining the authenticated admin client for scoped eval operations. | Route replay writes through the server-only writers; preserve its client-turn-id format under the new text-key uniqueness contract.                                                                                      | Replay-runner regression proves separate authenticated/admin and service-role writer clients plus ID compatibility. | Covered for legacy; worker routing remains                                              |

## Current security/write inventory

Phase 1 has moved every identified live application writer in this inventory to a server-only service-role boundary. The compatibility permissions below remain until Phase 2A revokes them after deployment verification and before worker execution is enabled:

| Surface                                           | Current authenticated capability                                                                    | Current callers                                                                                                                                                                                 | Required replacement                                                                                                                      |
| ------------------------------------------------- | --------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| `chat_turn_runs`                                  | Direct `INSERT` and `UPDATE` for owned rows.                                                        | Atomic admission uses the service-only RPC; status/event/prompt-snapshot/replay-source writes use the service-role observability writer. The authenticated grant remains unused by these paths. | Code cutover complete; revoke direct authenticated mutations in Phase 2A after deployment verification.                                   |
| `chat_turn_events`                                | Direct `INSERT` for owned rows.                                                                     | `TurnObservabilityWriter.flushTurnEvents` now runs through the `.server.ts` service-role writer; the authenticated grant remains.                                                               | Narrow server-only event RPC/writer; generation fence for worker mode.                                                                    |
| `chat_turn_checkpoints`                           | Direct `INSERT`, `SELECT`, and `UPDATE` for owned rows.                                             | Enforced `checkpoint-service.server.ts` receives the service-role client; the authenticated grant remains unused by the live route.                                                             | Code cutover complete; add worker generation fences and revoke direct authenticated mutation in Phase 2A.                                 |
| `agentic_chat_prepared_prompts`                   | Direct `INSERT`, `SELECT`, and `UPDATE` for owned rows.                                             | Prewarm and stream use `prepared-prompt-store.server.ts` with the service role; authenticated clients receive only bounded metadata.                                                            | Code cutover complete; revoke authenticated content DML in Phase 2A and introduce immutable input artifacts.                              |
| `cleanup_expired_agentic_chat_prepared_prompts()` | `authenticated` may execute a security-definer delete function.                                     | The existing cleanup scheduler is service-role owned; authenticated execute remains unused.                                                                                                     | Revoke authenticated execute in Phase 2A; add active-turn/input-artifact exclusion.                                                       |
| `chat_prompt_snapshots` (audit F7)                | Direct `INSERT` for owned rows.                                                                     | Stream route now calls scoped `TurnObservabilityWriter.persistPromptSnapshot` through the service-role client; authenticated INSERT remains unused but not revoked.                             | Code cutover complete; revoke the INSERT policy in Phase 2A after deployment verification.                                                |
| `queue_jobs` via `add_queue_job` (audit F3)       | Non-definer RPC invoked with the user-scoped client — implies authenticated INSERT on `queue_jobs`. | `project-calendar.service.ts`, `onto-event-sync.service.ts`.                                                                                                                                    | Definer RPC with a job-type allowlist (or revoked INSERT + definer enqueue paths) plus a non-service `agentic_chat_turn` rejection guard. |

The source evidence is in migrations `20260428000015`, `20260502000002`, and `20260523000000`. New worker-owned tables must begin with no client DML rather than repeating this compatibility window.

**Retained production capture 2026-07-30:** `docs/plans/evidence/agentic_chat_worker_phase0_preflight_prod_2026-07-30.json`. It confirms every authenticated capability in the table above against production, and clears the row hazards: zero duplicates on all four probes, `client_turn_id` UUID for all 592 non-null rows.

`supabase/tests/20260729000000_agentic_chat_worker_phase0.preflight.sql` makes this inventory repeatable against a target database. It reports duplicate command/running rows, RLS state, authenticated policies/effective privileges, routine exposure, constraints, and indexes without mutating data. The disposable-catalog smoke and retained production capture are complete; no additional Phase 0 target-database run is required unless the target schema changes before Phase 2A.

## The 12 locked corrections and proving tests

| Correction                                           | First proving phase | Required named test group                                                                                                 |
| ---------------------------------------------------- | ------------------- | ------------------------------------------------------------------------------------------------------------------------- |
| Database-enforced fencing and typed retry boundaries | 2B                  | `agentic_chat_worker_fencing`, `agentic_chat_retry_boundary`, `agentic_chat_stalled_recovery`                             |
| RPC-only control/prepared-input access               | 1 then 2A           | `legacy_server_writer_compat`, `agentic_chat_control_rls`, `prepared_prompt_lockdown`                                     |
| Lossless generation-aware reconnect                  | 2C/2D               | `agentic_chat_generation_reconcile`, `agentic_chat_reconcile_live_race`                                                   |
| Duplicate-first atomic admission                     | 1 and 2B            | `legacy_atomic_admission`, `worker_atomic_admission`, `worker_admission_hash_conflict`                                    |
| Immutable normalized history                         | 1 then 2A/2B        | `agentic_chat_input_artifact_history`, `agentic_chat_history_source_mutation`, `agentic_chat_prepared_history_divergence` |
| Durable trusted prepared context                     | 2A/2B               | `agentic_chat_input_artifact_prepared`, `prepared_cleanup_active_turn`                                                    |
| Terminal CAS and durable supersede                   | 2B/2D               | `agentic_chat_terminal_race`, `agentic_chat_supersede_wait`                                                               |
| Independent chat slots                               | 2B/3                | `chat_consumer_slot_isolation`                                                                                            |
| Stable mutation effect identity                      | 2B                  | `chat_turn_effect_lifecycle`, `chat_turn_effect_retry_receipt`                                                            |
| Server-authoritative transport                       | 2D                  | `agentic_chat_transport_lease`, `agentic_chat_lost_admission_response`                                                    |
| Bounded publisher/control loops                      | 2C                  | `chat_publisher_high_water`, `chat_cancel_batch_observer_100_turns`                                                       |
| Channel before admission; session inside admission   | 2D                  | `agentic_chat_session_inline_admission`, `agentic_chat_channel_ready_at_send`                                             |

## Phase 1 hosted gate attempt — FAIL 2026-07-31

The retained artifact is `docs/plans/evidence/agentic_chat_worker_phase1_gate_2026-07-31_0147cbd94.json`. It records clean `HEAD` `0147cbd94e85f406512245803758796145e0e950`, tree `49e74889c528eb0a475b9c0c257e75ff4eb9118e`, three repetitions, and Vitest retries disabled.

- 21/24 scenario executions passed; 27/30 turn assertions passed.
- All 30 turns reached terminal completion; there was one stream-error turn and zero capture-error turns.
- Recorded provider/model cost was `$0.12909659`.
- `project-organize` repetition 3 completed normally but did not group at least two original documents.
- `research-turn-finalizes` repetition 3 exhausted the existing provider stream retries and finished with one stream error.
- `task-reschedule-cold-reference` repetition 1 wrote August 5, 2026 instead of the required August 7, 2026 and finished at the tool-repetition limit.
- The other 21 scenario executions passed, including all restraint, multi-update, research readback, task completion/forward carry, and cold catch-up repetitions.
- Hosted admission itself was healthy across the corrected run: 30 timing samples, p50 111 ms, p95 187 ms, and max 206 ms. The retained Phase 0 comparator was p50 96 ms and p95 108 ms. Retained rows remained p95 105 per turn.

This is a hard-gate failure against the retained Phase 0 result below. Provider/model variance is plausible—especially for the exhausted stream—but cannot be assumed away. A targeted diagnostic may distinguish variance from a reproducible regression; only a subsequent full clean, retry-free 24/24 cohort can close Phase 1.

## Phase 0 hosted closure evidence

The final gate artifact is `docs/plans/evidence/agentic_chat_worker_phase0_gate_2026-07-31_0f63e47bb.json`. It records clean `HEAD` `0f63e47bbafc4e58d85b360b1edb1ef8d0fe3fb5`, tree `6e3f1b451e7920478f54fa36dddbb79dc68e7c83`, and retries disabled.

- All 24 scenario executions passed across the eight registered scenarios and three repetitions.
- All 30 turn assertions passed; all 30 turns reached terminal completion; stream errors and capture errors were both zero.
- The artifact includes legacy client/server timing distributions, 170 tool-execution samples, provider/model/cost attribution, and per-turn retained row/serialized-byte footprint.
- Research finalization/readback, forward carry, rescheduling, restraint, multi-update, organization, and cold catch-up each passed all registered repetitions.
- PostgreSQL WAL/statement rate remains a Phase 2 load-fixture measurement; the retained Phase 0 footprint is not a write-rate claim.

## Remaining Phase 0 exit gap

- Obtain final independent re-acceptance of contract revision .5, this ledger, the production preflight, and the retained correction/final-gate evidence. The first audit ran 2026-07-29; correction and re-audit passes closed all named findings, but the final .5 acceptance signature is not yet retained.
- Carry the locked P04/P12/P13/P27/P28 legacy fixtures into Phase 1 runtime differential comparisons after acceptance; no local legacy-side fixture gap remains for these rows.
