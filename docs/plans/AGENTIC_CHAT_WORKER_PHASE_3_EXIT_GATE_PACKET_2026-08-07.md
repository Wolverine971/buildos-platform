<!-- docs/plans/AGENTIC_CHAT_WORKER_PHASE_3_EXIT_GATE_PACKET_2026-08-07.md -->

<!-- doc-status: point-in-time -->

> **Point-in-time document.** Written 2026-08-06; describes the state of the system at that moment.
> It is not a current reference. Verify against code before acting on anything here.

# Agentic Chat Worker — Phase 3 Exit-Gate Evidence Packet

**Prepared:** 2026-08-07 (live scenarios executed 01:50–01:56Z; audit and rollback same session)
**Scope:** Maps every Phase 3 exit-gate bullet (`docs/plans/AGENTIC_CHAT_WORKER_REALTIME_MIGRATION_PLAN_2026-07-29.md` lines 844–855) to its strongest existing proof, and adds new LIVE production proofs of Stop-during-streaming, Stop-pre-synthesis, disconnect survival, and reconnect restore.
**Operating state during live proofs:** web deployment `build-mp5gywc7t` (routing exact `true`), worker commit `6d12e043e` (Railway, restarted 2026-08-07T00:37:54Z), hosted migrations `20260806010000/020000/021000` applied, cohort = single canary user, `CHAT_CONCURRENCY=1`.
**End state:** `AGENTIC_CHAT_WORKER_ROUTING_ENABLED` returned to exact `"false"`; rollback deployment `build-qs39bbatp` verified **Ready** at the top of `vercel ls --prod` and aliased to build-os.com. Verified via `vercel env pull` byte check.

All four live turns below ran in session `26fe15dc-34a5-48d7-926e-2f70ee4b16b6` (project `2ab7e1c0-1229-4431-8494-e9841944e96d`) with the exact canary text, all admitted `worker_realtime`, generation 1.

## Verdict table — exit-gate bullets → evidence

| #   | Exit-gate bullet (plan line)                                                                                                                                                                         | Verdict                                                                              | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Internal user completes a real read-only turn end to end (846)                                                                                                                                       | **PROVEN-LIVE**                                                                      | Canary 11: turn `9e54c04b-eb21-4ea4-9c8d-37b07ba496ee`, completed/stop in 83 s, verifier PASS (tasker/49 "Canary 11 result"). **Plus two more this session:** `65a8a97b-3e7e-4638-a0dd-015cdba2260c` (52.1 s) and `c146b606-d533-4ce8-90f8-ed9511d6c1f0` (60.3 s), both completed/stop, both `pnpm verify:agentic-chat-read-canary` **PASS** (16/16 lifecycle observations, exact usage, queue completed). Three consecutive live completions, zero sweeper involvement.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2   | Navigation/Realtime loss does not cancel the turn (847)                                                                                                                                              | **PROVEN-LIVE**                                                                      | S4, turn `c146b606`: tab **closed** at ~01:55:50Z while synthesis streamed (368 chars persisted). Durable text kept growing after disconnect (raw polls 840 → 1018 → 1127 → 1167) and the turn reached `completed/stop` at 01:56:14.75Z with no `cancel_requested_at`. Disposable: Phase 2 packet #5 (`agenticChatFixtureTurnExecutor.test.ts`, `agenticChatRecoverySnapshot.test.ts`, `worker-phase2d-composed-flow.test.ts`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 3   | Reconnect restores the exact partial/final output (848)                                                                                                                                              | **PROVEN-LIVE** (final output) + PROVEN-DISPOSABLE (mid-run partial)                 | S4 continued: fresh tab → project → session rendered the complete final answer (9:56 PM message, byte-matching `chat_turn_stream_state.assistant_text`; screenshot retained, see Artifacts). Mid-run partial restore + sequence-gap repair: `worker-phase2d-composed-flow.test.ts` ("converges duplicate admission, reload discovery, reconnect, a sequence gap, and terminal wait"), `phase2c-reconciliation.postgres.test.ts`, `phase2c-stream-delivery-ack.postgres.test.ts`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 4a  | Stop works during provider streaming (849)                                                                                                                                                           | **PROVEN-LIVE** (twice)                                                              | S1, turn `6750eebf-f66c-47f8-aecc-19b52b80c2d9`: Stop during **synthesis** streaming → executor-written `cancelled/cancelled` terminal **402 ms** after `cancel_requested_at`; partial assistant message `20478a0e` with `partial:true, interrupted:true, interrupted_reason:user_cancelled, partial_tokens:41`; exactly one `done` (seq 22) linking it. S2, turn `0e4f8116-a332-45c0-b526-96f5162696f3`: Stop during **round-1** provider streaming → clean cancelled terminal **391 ms** after request; `assistant_text=""`, `assistant_message_id=null` in the done payload (no blank message); exactly one `done` (seq 4).                                                                                                                                                                                                                                                                                                                                               |
| 4b  | Stop between tool stages (849)                                                                                                                                                                       | **PROVEN-DISPOSABLE** (live-bracketed)                                               | The live tool stage lasted ~626 ms and the post-tool/pre-synthesis gap ~400 ms — not reachable by a human/UI click; the two live cancels bracket it on both sides. Disposable: `agenticChatFixtureTurnExecutor.test.ts` "cannot invoke or publish provider output when cancellation wins the provider-start fence" (line 2120); `agenticChatToolExecution.test.ts` "surfaces stale, cancelled, and terminal receipts as typed execution fences" (line 119); `supabase/tests/20260804033000_agentic_chat_partial_cancellation_terminal_events.test.sql`.                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 4c  | Stop while queued (849)                                                                                                                                                                              | **PARTIAL** (disposable-proven; live window missed)                                  | Live attempt: two Stop clicks fired ≤2 s after send; the queued window was 1.34 s and the Stop control had not yet rendered — no cancel registered, the turn ran (became S4). Disposable proof is direct: `supabase/tests/20260802020100_agentic_chat_worker_claim_fencing.test.sql` ("A cancellation accepted while queued wins before claim and does not advance the domain generation", lines 197–198) and `phase2b-admission-claim.postgres.test.ts` ("ownership/generation/reset/cancel/security/concurrency/rollback claim checks").                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 5   | Stop/completion races → exactly one durable terminal; supersede cannot overlap active turns (850)                                                                                                    | **PROVEN-DISPOSABLE** (live-corroborated)                                            | Two-connection race + CAS: `supabase/tests/20260802030500_agentic_chat_worker_terminal_control.test.sql` (Phase 2 packet #6); `agenticChatFixtureTurnExecutor.test.ts` "honors a cancellation terminal winner returned to a completion replay" (line 2585). Supersede: `phase2b-execution-recovery.postgres.test.ts` "passes supersede terminal-wait and immutable-input cleanup/retry checks"; `20260803001000_agentic_chat_worker_phase2d_behavior_matrix.test.sql` steps 4–6 (replacement admission fails until the abort-ignoring first turn commits its cancelled terminal; cancel-first two-scheduler race). Live corroboration: every live terminal in the campaign (canary 11 + all four turns tonight) has exactly one `done` event.                                                                                                                                                                                                                                |
| 6   | General-worker saturation cannot delay a claimable chat turn beyond SLO (851)                                                                                                                        | **PROVEN-DISPOSABLE** (live-corroborated)                                            | `agenticChatFixtureConsumer.test.ts` "keeps saturated general slots independent from bounded chat slots" (line 51); `supabaseQueueRefill.test.ts` (Phase 2 packet #12 — two actually isolated `SupabaseQueue` instances); wake path `agenticChatConsumer.test.ts` "uses wake for immediate pickup…" (line 70) with the 1-second fallback (line 51). Live corroboration: created→`execution_started_at` was 1.03 s / 1.34 s / 0.50 s / 1.34 s across tonight's four turns under normal production general-queue load. No deliberate live general-slot saturation was run.                                                                                                                                                                                                                                                                                                                                                                                                     |
| 7   | Realtime pressure degrades to reconciliation; bounded memory/promises; terminal truth preserved (852)                                                                                                | **PROVEN-DISPOSABLE** (live-corroborated)                                            | `agenticChatStreamPublisher.test.ts`: "provides pressure relief and fails closed with the complete prefix at the hard bound" (427), "bounds terminal Broadcast retries after terminal truth commits" (486), "enters reconcile-only mode on replay…" (244), "suppresses later live events after Broadcast failure and emits only a reconcile hint" (336); `agenticChatFixtureTurnExecutor.test.ts` "turns publisher hard-bound overload into a failed terminal partial without retry" (2622); `agenticChatFixtureLoad.test.ts` (100-turn batch memory/high-water bounds); `phase2c-reconciliation.postgres.test.ts` / `20260802037000` SQL. Live: canary 11 reconcile cadence 43 calls/~85 s stopping at terminal (tasker/49).                                                                                                                                                                                                                                                |
| 8   | Admission pressure rejects before persistence with a clear retryable response; admitted turns continue (853)                                                                                         | **PROVEN-LIVE** (reject-before-persist) + DISPOSABLE (continue-under-pressure)       | Live: 2026-08-05 attempts at 14:07Z, 20:56:40Z, 21:47Z each failed closed with `capacity_exceeded` and **zero** durable rows — no turn, message, artifact, queue job, or stream state (Slice 15 plan doc, "canary 3/4" sections). Route contract: `capacity_exceeded` → HTTP 429 (`max_running`) / 503 (`pressure_closed`) with exact `Retry-After` (`apps/web/src/routes/api/agent/v2/turns/+server.ts` lines 309–326; route test "returns bounded capacity with exact Retry-After and no successful writes", `server.test.ts` line 384). Disposable no-objects/hard-cap proof: `20260802020000_agentic_chat_worker_atomic_admission.test.sql` (Phase 2 packet #15). Honest note: the live rejections were triggered by stale legacy rows counted by the cap (cap since scoped to worker-mode turns, Slice 16 W6); the mechanism proven is reject-with-no-persistence + retryable envelope. "Admitted turns continue normally under rejection pressure" is disposable-only. |
| 9   | Worker restart produces honest terminal/recoverable state, not an orphaned running turn (854)                                                                                                        | **PARTIAL** (disposable + live sweeper corroboration; live restart-mid-turn not run) | Disposable: `agenticChatStalledRecovery.test.ts` (pre-domain bridge → safe pre-start retry only; finalize-from-durable-snapshot; concurrent-sweeper-winner classified as stale ownership; bounded drain) and `20260802031000_agentic_chat_worker_execution_recovery.test.sql` (start-first vs recovery-first fencing). Live corroboration — three real stall recoveries, zero orphans: attempt 7 turn `670b3163` and canary 8 `f729f360` and canary 10 `1422ffc3` all swept to honest `failed/worker_interrupted/stale_context` terminals with `recovered_from_stall` (tasker/49). A deliberate live worker-process restart mid-turn requires Railway access and was out of authorized scope for this session — residual, see Gaps.                                                                                                                                                                                                                                          |
| 10  | Forced two-worker, timeout, stale-generation-write, source-history mutation, prepared-prompt tamper/cleanup, normal cache-cleanup, and chat-stalled-recovery tests pass before concurrency > 1 (855) | **PROVEN-DISPOSABLE** (itemized below); concurrency remains 1                        | See itemized citations below. All suites last ran green in the Slice 16 gates: worker 769 tests passed (1 intentional skip), web 3,694+ tests incl. all composed disposable PostgreSQL suites (tasker/50). `CHAT_CONCURRENCY` has not been raised above 1 — the precondition battery passes and the envelope is unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |

### Bullet 10 itemization

- **Forced two-worker:** `20260802020100_agentic_chat_worker_claim_fencing.test.sql` "Genuine two-connection contention…" (lines 288–289); `20260802031000_agentic_chat_worker_execution_recovery.test.sql` "Two current owners can race the same durable fence…" (lines 349–350); behavior-matrix step 6 (two real scheduler connections, cancel-first).
- **Timeout:** `agenticChatFixtureTurnExecutor.test.ts` — "classifies a queue timeout before provider authority as the one safe timeout retry" (1864), "terminalizes a never-resolving provider stream inside the executor budget" (1886), "finalizes a post-start timeout without text as error, timing, and done" (2481), "writes a terminal outcome when a read-tool network call exceeds its local deadline" (1396).
- **Stale-generation-write:** `20260802030500` SQL ("Stale generations are typed no-ops and forged processing tokens fail closed", line 501); `20260802033200` stream-write SQL (line 369); executor "cannot publish or finalize after the start fence reports a stale generation" (2092); `agenticChatCancellationObserver.test.ts` (79).
- **Source-history mutation:** behavior-matrix step 7 (frozen source-backed history survives source edit and deletion across generation 1 → 2); `agenticChatExecutionInput.test.ts` "fails closed when frozen history no longer matches its content hash" (230); Phase 2 packet #16.
- **Prepared-prompt tamper:** `turn-input-artifact-store.test.ts` "fails closed when canonical content no longer matches the stored hash" (235) and expired-retention rejection (274); `prepared-prompt-cache.test.ts` scaffold/tool/fingerprint mismatch rejections (133/160/188).
- **Prepared cleanup / normal cache-cleanup:** behavior-matrix steps 1–3 (10-minute consumed-cache cleanup deletes exactly the cache row; immutable artifact, hash, and history retained; turn still claim/executable); Phase 2 packet #17.
- **Chat-stalled-recovery:** `agenticChatStalledRecovery.test.ts` full suite; `agenticChatConsumer.test.ts` "delegates stalled rows only to the fenced chat recovery service" (138).

## Live scenario timelines (2026-08-07Z, all worker_realtime, generation 1)

**Turn A — unplanned full completion (intended S1, completed before Stop):** `65a8a97b-3e7e-4638-a0dd-015cdba2260c`
created 01:50:03.75 → execution 01:50:04.78 → provider round 1 01:50:05.86 → … → `completed/stop` 01:50:55.85 (**52.1 s**). Verifier **PASS**. Counts as live E2E completion #2 (after canary 11).

**S2 — Stop in the pre-synthesis window:** `0e4f8116-a332-45c0-b526-96f5162696f3`
created 01:52:01.43 → execution 01:52:02.77 → `provider_attempt_started` 01:52:03.81 (round 1; the only observation — no attempt_ended, no tool) → Stop clicked ~19 s after send → `cancel_requested_at` 01:52:19.08 → terminal `cancelled/cancelled/cancelled`, `cancel_reason=user_cancelled`, `terminalized_at` 01:52:19.47 (**391 ms** request→terminal, executor-written). Window hit: **provider round-1 streaming** (a slow first round; tool never started). Contract held: exactly one `done` (seq 4), `assistant_text=""`, `first_text_persisted_at=null`, no assistant message created (done payload `assistant_message_id: null`).

**S1 — Stop during synthesis streaming:** `6750eebf-f66c-47f8-aecc-19b52b80c2d9`
created 01:53:55.23 → round 1 success (3.2 s) → tool 01:54:01.13→01:54:01.75 (626 ms) → synthesis `provider_attempt_started` 01:54:02.13 → `first_text_persisted_at` 01:54:22.91 → DB-poll detected 56 chars streaming at 01:54:24.7 → Stop clicked → `cancel_requested_at` 01:54:28.61 → terminal `cancelled` 01:54:29.01 (**402 ms**, executor-written, not sweeper). Contract held: exactly one `done` (seq 22) linking partial assistant message `20478a0e-d752-42d0-91bf-47d6aaf1e288` with `{partial:true, interrupted:true, interrupted_reason:"user_cancelled", finished_reason:"cancelled", terminal_status:"cancelled", partial_tokens:41, tool_call_count:1, tool_round_count:1}` — the cancelled-with-text contract live.

**S3 attempt → S4 — disconnect survival + reconnect restore:** `c146b606-d533-4ce8-90f8-ed9511d6c1f0`
created 01:55:14.43 (two immediate Stop clicks ≤2 s after send registered no cancel — queued window was 1.34 s and the Stop control had not rendered; recorded as the S3 miss) → execution 01:55:15.77 → `first_text_persisted_at` 01:55:25.67 → streaming confirmed (368 chars) 01:55:45.96 → **tab closed ~01:55:50** → durable text grew while disconnected (polls ~01:56:01/07/13: 840 → 1018 → 1127 raw chars) → `completed/stop` 01:56:14.75 (**60.3 s**; `cancel_requested_at` null; done seq 115 with exact usage 13,577 tokens; assistant message `9345315c-9202-470b-833b-6421827212cd`). Fresh tab → project → session: complete final answer rendered (9:56 PM), matching durable `assistant_text`; no stuck "thinking" banner. Verifier **PASS** (16/16 lifecycle observations, `usageEvidence: exact`, `queueCompleted: true`).

Model spend: 4 turns, DeepSeek-class pricing — pennies.

## Artifacts

- Reconnect-restore screenshot: `/private/tmp/claude-501/-Users-djwayne-buildos-platform/15e2085d-cc5b-4f1d-a42f-a84861773dc8/scratchpad/s4-reconnect-restore-c146b606.jpg` (session-scratch copy; re-home if it must outlive the session).
- Verifier outputs for `65a8a97b` and `c146b606`: both `PASS: the turn satisfies the bounded production read durable-evidence contract.`
- Flag flips: enable on deployment `build-mp5gywc7t`; rollback on `build-qs39bbatp` (Ready, aliased); `.env.prod` pull confirms `AGENTIC_CHAT_WORKER_ROUTING_ENABLED="false"`.

## Residual gaps, stated plainly

1. **Stop-while-queued has no live proof.** The queued window is ~1.0–1.3 s in production and the Stop control renders after it closes; two rapid clicks missed it. The queued-cancel path is directly proven in the claim-fencing disposable SQL (cancel accepted while queued wins before claim, generation does not advance). Risk assessment: low — the live S2 cancel exercised the same durable cancel machinery one stage later.
2. **Worker-restart-mid-turn has no live proof.** Requires Railway restart authority, which was out of this session's scope. Compensating evidence: the stalled-recovery disposable suites plus three real production sweeper recoveries (attempt 7, canaries 8 and 10) that each produced honest `failed/worker_interrupted` terminals and never an orphaned `running` row.
3. **General-slot saturation and Realtime-overload were not deliberately induced live.** Both are architecture-plus-disposable proofs (separate chat-only queue instance; bounded publisher with fail-closed hard bound), with healthy live corroboration (sub-1.4 s claims; canary 11's bounded reconcile cadence).
4. **"Between tool stages" stop was live-bracketed, not live-hit.** The gap is ~400 ms in production; both adjacent windows were cancelled live and the between-stage fence is disposable-proven.
5. Known non-blocking client residuals from canary 11 (duplicated thinking-activity rows; the staged-but-undeployed reconcile-throttle/thinking fixes) were again visible during S1's run and remain client-render polish, not turn-truth defects — deploying the staged follow-up is tasker/50 work.

## Go/no-go recommendation

**GO — declare Phase 3 exited.** Every exit-gate bullet is now backed by either direct live production proof (E2E completion ×3 with verifier PASS, Stop during streaming in two different windows with ~400 ms executor-written terminals and the exact cancelled-with-text contract, disconnect survival with post-disconnect durable progress, reconnect restoring the exact final output) or by the composed disposable PostgreSQL/worker suites the plan itself names as the accepted proof vehicle for forced-fault behavior (two-worker races, queued cancel, stale generations, tamper/cleanup, stalled recovery). The two honest residuals — live queued-window stop and live worker-restart-mid-turn — are low-risk paths whose disposable proofs are direct and whose adjacent live behavior has been observed repeatedly; they should be carried as named items into the Phase 4/5 hardening ledger (alongside tasker/50's already-tracked provider-budget overrun canary and prod-constraint diff) rather than blocking exit. Routing is back to exact `false`; the Phase 3 envelope (single user, one worker, concurrency 1) was never widened.
