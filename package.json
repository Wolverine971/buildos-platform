{
	"name": "buildos-platform",
	"version": "0.0.0",
	"private": true,
	"description": "BuildOS Platform - AI-powered productivity platform monorepo",
	"scripts": {
		"dev": "turbo dev",
		"build": "turbo build",
		"test": "turbo test",
		"test:run": "turbo test:run",
		"test:coverage": "turbo test:coverage",
		"test:schema-tools": "node --import tsx --test scripts/lib/supabase-openapi-types.test.ts",
		"test:agentic-chat-read-canary": "node --import tsx --test scripts/lib/agentic-chat-read-canary.test.ts",
		"verify:agentic-chat-read-canary": "node --import tsx scripts/verify-agentic-chat-read-canary.ts",
		"lint": "turbo lint",
		"lint:fix": "turbo lint:fix",
		"typecheck": "turbo typecheck",
		"format": "prettier --write .",
		"format:changed": "node --import tsx scripts/formatChanged.ts",
		"clean": "turbo clean",
		"pre-push": "pnpm run check:doc-health:strict && turbo typecheck test:run lint build",
		"gen:types": "node --import tsx scripts/generate-types.ts",
		"check:supabase-rpc-drift": "node scripts/security/check-supabase-rpc-drift.mjs",
		"check:doc-health": "node scripts/docs/check-doc-health.mjs",
		"check:doc-health:strict": "node scripts/docs/check-doc-health.mjs --strict",
		"fix:doc-health": "node scripts/docs/check-doc-health.mjs --stamp",
		"gen:schema": "node --import tsx scripts/extract-database-schema.ts && node --import tsx scripts/generate-supabase-skill-schema.ts",
		"gen:skill-schema": "node --import tsx scripts/generate-supabase-skill-schema.ts",
		"gen:web": "pnpm --filter=web gen:web",
		"gen": "node --import tsx scripts/run-gen.ts",
		"gen:all": "pnpm run gen:types -- --allow-stale && pnpm run gen:schema && pnpm run gen:web && pnpm --filter=@buildos/shared-types build && pnpm run label-paths:changed && pnpm run format:changed && pnpm typecheck",
		"gen:all:full": "pnpm run gen:types -- --allow-stale && pnpm run gen:schema && pnpm run gen:web && pnpm --filter=@buildos/shared-types build && pnpm run label-paths && pnpm format && pnpm typecheck",
		"label-paths": "node --import tsx scripts/labelFilePaths.ts",
		"label-paths:changed": "node --import tsx scripts/labelFilePaths.ts --changed"
	},
	"devDependencies": {
		"dotenv": "^16.4.5",
		"prettier": "^3.6.2",
		"prettier-plugin-svelte": "^3.4.0",
		"tsx": "^4.20.3",
		"turbo": "^2.10.5"
	},
	"packageManager": "pnpm@11.7.0",
	"engines": {
		"node": ">=20.19.0",
		"pnpm": ">=11.0.0"
	}
}
